import React from "react";
import { CardLevel} from "./GameTypes";
const Level: React.FC<{cardLevel: CardLevel}> = (props) => {
    return (
        <div>
            <h1>CardLevel</h1>
        </div>
    );
}
export default Level;